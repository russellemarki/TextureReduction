#include "math.h"


double almnsp(const size_t l, const size_t m, const size_t n, const size_t s);
double blmn(const size_t l, const size_t m, const size_t nu);
size_t cubLin(const size_t l);

